package GestionEmpleados;

/**
 * Enum que define los tipos de rol que puede tener un empleado.
 * 
 * ADMINISTRADOR: acceso total al sistema.
 * USUARIO: acceso limitado.
 *
 * @author [Eloy]
 * @version 1.0
 * @since 1.0
 */
public enum Rol {
	ADMINISTRADOR, USUARIO
}
